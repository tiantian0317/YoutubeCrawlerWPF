<Window x:Class="YoutubeCrawlerWPF.DataViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="数据库数据查看 - YouTube数据分析系统" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#f5f5f5">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="#2196F3" Padding="15" Margin="0,0,0,10" CornerRadius="5">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="📊 " FontSize="24" Foreground="White" VerticalAlignment="Center"/>
                <TextBlock Text="数据库数据查看" 
                           Foreground="White" 
                           FontSize="20" 
                           FontWeight="Bold"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- 控制栏 -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Label Content="查看数据表:" FontSize="14" VerticalAlignment="Center"/>
                <ComboBox x:Name="cbTableSelect" Width="200" Margin="5" SelectionChanged="cbTableSelect_SelectionChanged">
                    <ComboBoxItem Content="📋 任务 (Tasks)"/>
                    <ComboBoxItem Content="📺 频道 (Channels)"/>
                    <ComboBoxItem Content="🎬 视频 (Videos)"/>
                    <ComboBoxItem Content="📝 播放列表 (Playlists)"/>
                </ComboBox>
                <Button Content="🔄 刷新" Click="Refresh_Click" Width="80" Margin="5,0,10,0"/>
                <TextBlock x:Name="txtRecordCount" Text="记录数: 0" FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center" FontWeight="Bold"/>
            </StackPanel>

            <Button Grid.Column="2" Content="❌ 关闭" Click="Close_Click" Width="80" Margin="10,0,0,0"/>
        </Grid>

        <!-- 数据表格 -->
        <Border Grid.Row="2" Background="White" CornerRadius="5" BorderBrush="#ddd" BorderThickness="1" Margin="10">
            <DataGrid x:Name="dgData"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="True"
                      CanUserResizeColumns="True"
                      CanUserSortColumns="True"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      GridLinesVisibility="All"
                      HeadersVisibility="Column"
                      AlternatingRowBackground="#f5f5f5"
                      RowBackground="White"
                      FontSize="12"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      FrozenColumnCount="1">
                
                <DataGrid.Resources>
                    <!-- 选中行样式 -->
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#B3E5FC"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                    
                    <!-- 列标题样式 -->
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#2196F3"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10,5"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    </Style>
                </DataGrid.Resources>
            </DataGrid>
        </Border>

        <!-- 状态栏 -->
        <StatusBar Grid.Row="3" Background="#E3F2FD">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="💡 " FontSize="14"/>
                    <TextBlock x:Name="statusText" Text="就绪" FontSize="14" Margin="5,0"/>
                    <Separator Margin="10,0" Width="1" Background="#fff"/>
                    <TextBlock Text="🔄 点击刷新更新数据 | 📊 支持横向和纵向滚动" FontSize="12" Foreground="#666" Margin="10,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
