<Window x:Class="YoutubeCrawlerWPF.DetailViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="数据详细信息" Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#f5f5f5">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="#2196F3" Padding="15" Margin="0,0,0,10" CornerRadius="5">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock x:Name="txtTitle" Text="数据详情" 
                           Foreground="White" 
                           FontSize="18" 
                           FontWeight="Bold"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- 详细数据列表 -->
        <Border Grid.Row="1" Background="White" CornerRadius="5" BorderBrush="#ddd" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="spDetails" Margin="15">
                    <!-- 动态添加详细数据项 -->
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- 底部按钮 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="📋 复制全部" Click="CopyAll_Click" Width="100" Margin="5" Background="#4CAF50" Foreground="White"/>
            <Button Content="❌ 关闭" Click="Close_Click" Width="80" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
